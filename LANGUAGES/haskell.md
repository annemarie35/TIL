# HASKELL

- Tutoriel https://www.hexbites.dev/stories/2024-10-06-ghcup-macos.html
- Installer les outils via l'interface cli `ghcup tui`
- Vérifier ce qui est réellement dans ton dossier GHCup
  `ls ~/.ghcup/bin/`

- `ghcup whereis stack`
- ajouter dans `vi  ~/.zshrc` `export PATH="$HOME/.ghcup/bin:$PATH"
`